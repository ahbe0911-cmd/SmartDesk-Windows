# Security

لطفاً رمز عبور، توکن، کلید API، فایل نشست یا اطلاعات محرمانه را داخل Issue یا مخزن قرار ندهید.

SmartDesk اعتبار گواهی SSL سایت‌ها را دور نمی‌زند. اگر سایتی گواهی نامعتبر داشته باشد، WebView2 هشدار استاندارد مرورگر را نمایش می‌دهد. گزارش‌های خطا فقط روی همان رایانه و در مسیر زیر ذخیره می‌شوند:

```text
%LOCALAPPDATA%\SmartDesk\Logs
```
